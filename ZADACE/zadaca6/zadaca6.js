// Unos broja od korisnika
let broj = prompt("Unesite broj:");

// Pretvaranje unosa u broj
broj = Number(broj);

// Provjera je li broj veći od 10
if (broj > 10) {
    alert("Broj je veći od 10");
} else {
    alert("Broj nije veći od 10");
}